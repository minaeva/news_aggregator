version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  pre_build:
    commands:
      - echo Pre-build steps...
      - cd user-service
      - ./gradlew clean
  build:
    commands:
      - echo Build of User service started on `date`
      - ./gradlew build -x test
  post_build:
    commands:
      - echo Build completed on `date`
      - echo "Listing contents of the user-service directory:"
      - ls -l user-service/
      - echo "Listing contents of the build directory:"
      - ls -l user-service/build/
      - cp user-service/build/libs/*.jar .
      - chmod +x start_application.sh
      - zip -r user-deployment-package.zip *.jar appspec.yml start_application.sh

artifacts:
  files:
    - user-deployment-package.zip

cache:
  paths:
    - '/root/.gradle/caches/**/*'
